{
    frankenphp
    admin off
    auto_https off
}

:{$PORT:8080} {
    root * /app/public
    encode zstd br gzip

    # Archivos estáticos del SPA React (public/spa/)
    handle /spa/* {
        file_server
    }

    # Todo lo demás → PHP (Laravel maneja la ruta)
    php_server
}
